# Hand Constellation DocC 튜토리얼 개편 계획

> 작성일: 2026-08-31  
> 상태: 수정 전 계획 확정안  
> 대상: DocC 튜토리얼 구조, 단계별 코드 자료, 튜토리얼 이미지, 커스텀 Documentation Theme  
> 제외: 실제 visionOS 앱의 기능 및 Swift 구현 변경

## 1. 문서 목적

현재 하나의 긴 페이지로 구성된 Hand Constellation DocC 튜토리얼을 여러 Chapter와 짧은 튜토리얼 페이지로 재구성한다.

개편의 핵심은 다음과 같다.

1. 사용자가 전체 학습 위치와 남은 진도를 Chapter 단위로 파악할 수 있게 한다.
2. 새 파일 생성과 기존 파일 수정을 명확히 분리한다.
3. 현재 단계에서 추가하는 코드가 오른쪽 코드 화면에 자동으로 보이게 한다.
4. 어려운 ARKit, RealityKit, 상태 머신 개념을 사용하기 전에 설명한다.
5. Xcode와 Apple Vision Pro에서 수행할 작업을 실제 화면 중심으로 안내한다.
6. 구현 과정과 기기 실행·권한 허용·완성 동작 검증을 서로 다른 Chapter로 분리한다.

## 2. 현재 상태와 문제

현재 튜토리얼은 `BuildingHandConstellation.tutorial` 한 파일에 8개 Section과 78개 Step이 들어 있다. 코드 변경은 작은 단위로 표시되지만 전체 과정이 한 페이지에 이어져 다음 문제가 남아 있다.

- 전체 과정이 길어 현재 학습 위치와 완료 범위를 파악하기 어렵다.
- 오른쪽 코드 패널이 새 코드로 교체되어도 패널 내부 스크롤 위치가 유지된다.
- 새 Swift 파일을 만드는 단계와 이미 존재하는 파일을 수정하는 단계가 같은 Section에 섞여 있다.
- 구현과 Apple Vision Pro 실행 과정이 같은 흐름에 들어 있다.
- ARKit, RealityKit, `simd`, 체류 판정, 상태 전환을 처음 접하는 학습자에게 진도가 빠르다.
- 현재 이미지는 전체 구조를 설명하는 개념도가 중심이며 Xcode 조작 화면이 부족하다.

## 3. 반영할 피드백과 완료 기준

| 번호 | 피드백 | 수정 방향 | 완료 기준 |
| --- | --- | --- | --- |
| 1 | 왼쪽 Step과 오른쪽 코드 스크롤이 연동되지 않음 | 현재 Step의 강조 코드 영역으로 자동 이동하는 테마 스크립트 추가 | Step이 바뀐 뒤 강조된 코드가 오른쪽 패널 안에 자동으로 표시됨 |
| 2 | 새 파일 생성과 기존 파일 수정이 구분되지 않음 | 파일 생성만 다루는 독립 튜토리얼 페이지 배치 | 파일 생성 페이지에는 기존 파일 수정 지시가 들어가지 않음 |
| 3 | 구현과 Vision Pro 실행이 함께 있음 | 실행과 권한 허용을 마지막 독립 Chapter로 이동 | 구현 Chapter에는 기기 실행 절차가 들어가지 않음 |
| 4 | 전반적인 난이도가 높음 | 사전 요구사항, 용어 설명, 작은 코드 Step, 중간 확인 단계 추가 | 한 Step에서 한 행동 또는 하나의 논리만 다룸 |
| 5 | 텍스트만으로 이해하기 어려움 | Xcode 실제 캡처와 단계별 개념도 추가 | 모든 UI 조작 페이지와 어려운 개념 페이지에 시각 자료가 있음 |
| 6 | 전체 과정이 한 페이지에 있음 | 여러 `@Chapter`와 `@TutorialReference`로 분리 | 목차에서 Chapter별 진도와 개별 튜토리얼 페이지를 선택할 수 있음 |

## 4. 목표 DocC 정보 구조

DocC의 계층을 다음과 같이 사용한다.

```text
@Tutorials  전체 Hand Constellation 과정
├── @Chapter 1  프로젝트 시작하기
│   ├── @TutorialReference  준비 사항 확인하기
│   ├── @TutorialReference  visionOS 프로젝트 만들기
│   ├── @TutorialReference  기본 코드 읽고 실행하기
│   └── @TutorialReference  손 추적 권한 설명 추가하기
├── @Chapter 2  앱의 상태와 화면 만들기
├── @Chapter 3  손 추적 구현하기
├── @Chapter 4  점 입력과 손짓 구현하기
├── @Chapter 5  별자리 모델과 화면 구현하기
├── @Chapter 6  전체 기능 연결하기
└── @Chapter 7  Vision Pro에서 실행하기
```

- `@Tutorials`: 전체 과정의 소개와 최종 결과를 보여준다.
- `@Chapter`: 같은 학습 목표를 가진 튜토리얼 페이지를 묶는다.
- `@Tutorial`: 사용자가 한 번에 완료할 수 있는 한 페이지다.
- `@Section`: 한 페이지 안에서 필요한 짧은 설명과 Step을 묶는다.
- `@Step`: 한 번의 UI 행동 또는 하나의 코드 변경만 다룬다.

전체 목표는 7개 Chapter, 약 22~25개 튜토리얼 페이지, 180~210분이다. 코드 스냅샷 감사 결과에 따라 페이지 수는 2개 범위 안에서 조정할 수 있지만 Chapter의 책임은 변경하지 않는다.

## 5. Chapter별 개편안

### Chapter 1. 프로젝트 시작하기

목표: 프로젝트를 만들고 Xcode가 생성한 코드를 이해한 뒤 손 추적 사용 목적을 설정한다.

예정 페이지:

1. **시작하기 전에**
   - 난이도: 중급 이상
   - 필요한 Swift·SwiftUI 사전 지식
   - ARKit과 RealityKit은 과정 안에서 설명한다는 안내
   - 실제 손 추적 최종 검증에는 Apple Vision Pro가 필요하다는 안내
   - Simulator에서 확인 가능한 범위와 불가능한 범위
2. **visionOS 프로젝트 만들기**
   - visionOS App 템플릿 선택
   - Product Name, Interface, Language 설정
   - 프로젝트 저장 위치 확인
3. **Xcode 기본 코드 읽고 실행하기**
   - `ContentView.swift` 읽기
   - 앱 진입점 파일 읽기
   - Apple Vision Pro Simulator에서 `Hello, world!` 확인
4. **손 추적 권한 설명 추가하기**
   - Target의 Info 화면에서 항목 추가
   - 사용자에게 보이는 설명 문구 입력
   - 마지막에만 `Info.plist` Source Code 표현 확인

완료 확인:

- 기본 앱이 Simulator에서 실행된다.
- 권한 설명이 Xcode Target의 Info 화면에 표시된다.
- 학습자가 `<key>`를 직접 외워 입력하지 않는다.

### Chapter 2. 앱의 상태와 화면 만들기

목표: 공통 상태와 제어 창을 만든다. 아직 손 추적이나 RealityKit 장면은 실행하지 않는다.

예정 페이지:

1. **새 파일 준비하기: 앱 상태**
   - `AppModel.swift` 생성
   - 파일 이름과 Target Membership 확인
   - Xcode가 넣은 초기 코드 확인
2. **AppModel의 상태를 작은 단위로 추가하기**
   - `@Observable`과 `@MainActor`
   - 몰입형 공간 상태
   - 손 추적 상태
   - 그리기 활성화 상태
   - 점·별자리 개수와 초기화 요청
3. **기존 ContentView를 ControlView로 바꾸기**
   - 파일과 struct 이름 변경
   - 제목과 안내 문구
   - 시작·종료 버튼
   - 그리기 전환과 초기화 UI

완료 확인:

- 새 파일을 만드는 페이지와 기존 파일을 수정하는 페이지가 분리되어 있다.
- `AppModel`의 각 상태가 필요한 이유를 설명할 수 있다.
- 아직 구현하지 않은 손 추적 기능을 실행하지 않는다.

### Chapter 3. 손 추적 구현하기

목표: ARKit 세션을 시작하고 오른손 검지 끝 위치를 월드 좌표로 변환한다.

예정 페이지:

1. **새 파일 준비하기: HandTrackingService**
   - `HandTrackingService.swift` 생성
   - 초기 import와 파일 위치 확인
2. **ARKit 손 추적 세션 시작하기**
   - `ARKitSession`과 `HandTrackingProvider`의 역할
   - 기능 지원 확인
   - 권한 상태 조회와 요청
   - 추적 시작과 종료
3. **오른손 검지 끝을 월드 좌표로 바꾸기**
   - `HandAnchor`와 손 골격 설명
   - 오른손과 추적 상태 검사
   - `.indexFingerTip` 관절 찾기
   - 손 앵커 변환과 관절 변환 곱하기
   - x, y, z 위치 꺼내기

완료 확인:

- 권한, 기능 지원, 추적 상태를 서로 구분한다.
- 월드 좌표 변환을 코드보다 먼저 그림으로 설명한다.
- 한 Step에서 하나의 guard 또는 한 번의 계산만 추가한다.

### Chapter 4. 점 입력과 손짓 구현하기

목표: 검지 체류를 한 번의 점 입력으로 바꾸고 주먹 유지로 그리기 상태를 전환한다.

예정 페이지:

1. **새 파일 준비하기: 입력 감지기**
   - `DwellDetector.swift` 생성
   - `FistHoldDetector.swift` 생성
   - 두 파일의 책임 비교
2. **검지가 머무르면 한 번만 점 확정하기**
   - `idle`, `dwelling`, `coolingDown` 설명
   - 0.8초 체류
   - 1.5cm 안정 반경
   - 3cm 재활성화 거리
   - 추적 손실 시 취소
3. **주먹을 유지하면 그리기 전환하기**
   - `ready`, `holding`, `waitingForRelease` 설명
   - 0.6초 진행률
   - 한 번의 주먹 유지에서 한 번만 전환
   - 손을 다시 편 뒤 재활성화
   - 주먹 감지 중 체류 입력 취소

완료 확인:

- 검지를 이동하는 동안 점이 생성되지 않는다.
- 같은 위치에서 점이 반복되지 않는다.
- 주먹 손짓 자체가 별자리 점으로 기록되지 않는다.

### Chapter 5. 별자리 모델과 화면 구현하기

목표: 점을 별자리 단위로 저장하고 RealityKit 엔티티로 점과 선을 표현한다.

예정 페이지:

1. **새 파일 준비하기: 별자리 데이터와 화면**
   - `ConstellationConfiguration.swift` 포함 여부를 실제 앱 코드와 대조해 확정
   - `ConstellationModel.swift` 생성
   - `ConstellationRenderer.swift` 생성
   - 데이터 책임과 화면 책임 비교
2. **조절 가능한 값을 한곳에 모으기**
   - 체류 시간, 거리, 점 크기, 선 굵기의 단위 설명
   - 실제 앱과 최종 튜토리얼 스냅샷이 같은 구성 방식을 사용하도록 정리
3. **여러 별자리를 나누어 저장하기**
   - 점 배열과 별자리 배열 차이
   - 첫 점과 다음 점
   - 이전 점과 새 점의 선분
   - 그리기를 끌 때 현재 별자리 끝내기
   - 다시 켰을 때 새 별자리 시작하기
4. **RealityKit으로 점과 선 그리기**
   - root, 점 컨테이너, 선 컨테이너
   - 구 mesh와 material
   - 두 점의 중점과 거리
   - 원기둥 회전
   - 체류 cursor와 전체 초기화

완료 확인:

- 두 번째 별자리의 첫 점이 첫 번째 별자리와 연결되지 않는다.
- 데이터 모델과 렌더러의 책임이 섞이지 않는다.
- RealityKit 엔티티 구조를 코드보다 먼저 그림으로 설명한다.

### Chapter 6. 전체 기능 연결하기

목표: 추적, 손짓, 체류, 모델, 렌더러를 안전한 순서로 연결하고 몰입형 공간의 생명주기에 묶는다.

예정 페이지:

1. **새 파일 준비하기: 몰입형 흐름**
   - `ImmersiveCoordinator.swift` 생성
   - `ImmersiveView.swift` 생성
   - 두 파일의 책임 비교
2. **입력 처리 순서 연결하기**
   - 손 추적 시작
   - 주먹 판정을 가장 먼저 수행
   - 그리기 비활성화를 점 입력보다 먼저 검사
   - 검지 위치를 체류 감지기로 전달
   - 확정된 위치만 모델과 렌더러에 전달
   - 그리기를 끌 때 현재 별자리 종료
3. **RealityView와 앱 진입점 연결하기**
   - renderer root 추가
   - `.task`, `.onChange`, `.onDisappear`
   - `WindowGroup`과 `ImmersiveSpace`
   - `AppModel` 환경 공유

완료 확인:

- 종료하거나 주먹을 쥐는 동안 점이 추가되지 않는다.
- 몰입형 공간을 닫으면 추적 task가 정리된다.
- Chapter가 끝나는 시점에 전체 앱 코드가 빌드 가능하다.

### Chapter 7. Vision Pro에서 실행하기

목표: 실제 기기에서 권한을 허용하고 두 개의 별자리를 만들며 전체 기능을 확인한다.

예정 페이지:

1. **Vision Pro에서 실행하고 손 추적 권한 허용하기**
   - Signing과 Bundle Identifier 확인
   - Apple Vision Pro 실행 대상 선택
   - 앱 실행
   - 손 추적 권한 창 설명
   - 허용과 거부 결과 비교
2. **다섯 개의 점으로 첫 별자리 만들기**
   - 공간 진입 직후 그리기 꺼짐 확인
   - 주먹을 0.6초 유지해 켜기
   - 검지를 0.8초 머물러 점 생성
   - 3cm 이상 이동한 뒤 다음 점 생성
   - 다섯 번째 점과 선 확인
3. **두 번째 별자리와 초기화 확인하기**
   - 주먹으로 그리기 끄기
   - 손을 움직여도 여섯 번째 점이 생기지 않는지 확인
   - 다시 켜고 두 번째 별자리 시작
   - 두 별자리 사이에 선이 없는지 확인
   - 초기화와 자주 발생하는 오류 확인

완료 확인:

- 실행과 권한 허용 과정이 구현 Chapter에 섞이지 않는다.
- 실제 기기에서 그리기 꺼짐 초기값을 확인한다.
- 한 세션에서 서로 연결되지 않은 두 별자리를 만든다.

## 6. 페이지와 Step 작성 규칙

### 6.1 페이지 규칙

- 한 페이지는 하나의 명확한 학습 목표만 가진다.
- 예상 소요 시간은 5~15분 범위로 맞춘다.
- 페이지 시작에 **이번 단계에서 만들 것**을 적는다.
- 페이지 끝에 **결과 확인**과 **문제가 생겼다면**을 둔다.
- 다음 페이지에서 사용할 용어는 현재 페이지에서 먼저 소개한다.
- 구현하지 않은 타입을 참조해 중간 빌드가 깨지는 순서를 피한다.

### 6.2 새 파일 생성 페이지 규칙

- 새 파일 생성 페이지에는 기존 파일 수정 코드를 넣지 않는다.
- Xcode의 New File 메뉴, 템플릿, 파일 이름, Target Membership을 보여준다.
- 관련 파일을 함께 준비할 수 있지만 생성 후 구현은 다음 페이지에서 시작한다.
- 첫 코드 스냅샷은 `reset: true`로 표시한다.
- 빈 파일이나 Xcode 초기 코드가 왜 보이는지 설명한다.

### 6.3 코드 Step 규칙

- 한 Step은 한 행동 또는 하나의 코드 논리를 다룬다.
- 일반적인 Step에서는 1~5줄을 추가한다.
- enum이나 switch처럼 분리하면 오히려 이해가 어려운 경우에만 한 논리 블록을 함께 추가한다.
- 모든 수정 스냅샷은 `previousFile`을 연결한다.
- 새로 추가된 줄이 파란색으로 강조되는지 확인한다.
- 코드 뒤에는 문법 설명보다 해당 코드가 앱 동작에 미치는 영향을 먼저 쓴다.
- 긴 메서드는 선언, 입력 검사, 계산, 상태 변경, 반환으로 나눈다.
- 각 튜토리얼 마지막 코드 스냅샷은 다음 튜토리얼의 시작 스냅샷과 정확히 이어져야 한다.

### 6.4 난이도 완화 규칙

- 과정 첫 화면에 **중급 이상 난이도**를 명시한다.
- Swift 문법, SwiftUI, ARKit, RealityKit 개념을 구분한다.
- ARKit, RealityKit, `simd`, actor, async sequence, 상태 머신은 처음 사용하기 전에 설명한다.
- 미터 단위 값을 센티미터로 함께 표기한다.
- `guard`가 연속될 때는 각 조건의 실패 상황을 설명한다.
- 각 Chapter 끝에 핵심 개념 점검 또는 간단한 Assessment를 둔다.

## 7. 오른쪽 코드 자동 스크롤 계획

### 7.1 현재 동작

DocC 렌더러는 왼쪽의 현재 Step을 감지해 오른쪽 코드 자료를 교체한다. 그러나 `.code-preview`는 독립적인 스크롤 영역이므로 이전 코드의 스크롤 위치가 남을 수 있다.

### 7.2 구현 방식

기존의 해시가 붙은 DocC 번들 파일을 직접 수정하지 않는다. 별도의 읽기 쉬운 스크립트를 추가하고 `DocumentationTheme/index-template.html`에서 불러온다.

예정 파일:

```text
DocumentationTheme/js/handconstellation-step-sync.js
```

동작 순서:

1. 왼쪽 `.step.focused`의 변경을 감지한다.
2. 오른쪽 `.code-preview`의 코드 교체가 끝날 때까지 다음 렌더 시점을 기다린다.
3. `.code-line-container.highlighted` 요소를 찾는다.
4. 강조 블록 전체가 보이도록 오른쪽 코드 패널의 `scrollTop`을 계산한다.
5. 강조 블록이 화면보다 작으면 코드 패널 중앙에 배치한다.
6. 강조 줄이 없는 새 코드 자료는 코드 상단으로 이동한다.
7. 한 Step에서 한 번만 자동 이동하고 이후 사용자의 수동 스크롤은 방해하지 않는다.
8. 이미지 Step과 코드가 없는 Step에서는 코드 패널을 강제로 이동하지 않는다.
9. 모바일 코드 모달에는 데스크톱 동기화 로직을 적용하지 않는다.
10. 모션 감소 설정에서는 애니메이션 없이 즉시 이동한다.

### 7.3 스크롤 완료 기준

- 짧은 코드와 긴 코드 모두에서 강조된 줄이 보인다.
- 같은 파일의 아래쪽 줄로 이동하는 Step에서도 정상 동작한다.
- 다른 코드 파일로 바뀌면 이전 파일의 스크롤 위치를 사용하지 않는다.
- 사용자가 오른쪽 코드를 수동으로 스크롤한 뒤에도 다음 Step에서만 다시 동기화된다.
- 왼쪽 페이지 자체의 위치가 오른쪽 코드 이동 때문에 흔들리지 않는다.
- 좁은 화면과 모바일 레이아웃의 기존 동작을 깨뜨리지 않는다.

## 8. 이미지와 캡처 제작 계획

### 8.1 이미지 원칙

- Xcode와 시스템 권한 화면은 실제 캡처를 사용한다.
- 시스템 화면처럼 보이는 가짜 권한 이미지는 만들지 않는다.
- 설명용 그림은 기존 이미지를 재사용하거나 같은 Apple 스타일로 다시 정리한다.
- 한 이미지에는 하나의 핵심 행동만 강조한다.
- 강조색은 현재 Step 표시와 같은 파란색 계열을 사용한다.
- 불필요한 계정명, 경로, 기기 식별 정보는 잘라낸다.
- 모든 이미지에 구체적인 한국어 대체 텍스트를 작성한다.
- 코드 한 줄 추가처럼 오른쪽 diff만으로 충분한 Step에는 중복 이미지를 넣지 않는다.

### 8.2 필수 캡처 목록

| 구분 | 이미지 |
| --- | --- |
| 프로젝트 | visionOS App 템플릿 선택 화면 |
| 프로젝트 | Product Name, SwiftUI, Swift 설정 화면 |
| Xcode | Project navigator의 기본 파일 위치 |
| Xcode | 기본 앱의 Simulator 실행 결과 |
| 권한 | Target의 Info 탭과 Hand Tracking Usage Description 추가 위치 |
| 파일 생성 | New File 메뉴와 Swift File 선택 화면 |
| 파일 생성 | 파일 이름과 Target Membership 확인 화면 |
| 앱 구조 | WindowGroup, AppModel, ImmersiveSpace 관계도 |
| 손 추적 | 오른손 골격과 indexFingerTip 위치 그림 |
| 좌표 | 손 앵커 변환과 관절 변환의 곱 설명 그림 |
| 체류 | 0초부터 0.8초까지의 진행 과정 |
| 손짓 | 주먹 유지와 손을 다시 펴는 상태 흐름 |
| 데이터 | 여러 별자리를 저장하는 이중 배열 그림 |
| RealityKit | root, 점, 선, cursor 엔티티 트리 |
| 기기 실행 | Xcode의 Apple Vision Pro 실행 대상 선택 |
| 권한 | 실제 기기의 손 추적 권한 창 |
| 완성 동작 | 다섯 점으로 만든 첫 별자리 |
| 완성 동작 | 그리기 꺼짐과 분리된 두 번째 별자리 |

예상 이미지 수는 18~24개다. 모든 튜토리얼 페이지에 억지로 이미지를 하나씩 넣기보다 UI 조작과 개념 이해에 실제로 필요한 위치를 우선한다.

### 8.3 기기 전용 이미지 처리

- Apple Vision Pro 권한 창과 손 추적 결과는 실제 기기에서 캡처한다.
- 기기 캡처를 바로 확보할 수 없으면 문서 구조와 파일 이름을 먼저 확정하고 해당 Step을 캡처 대기 상태로 표시한다.
- 최종 공개 전에는 임시 그림을 실제 기기 캡처로 교체한다.

## 9. 코드 스냅샷 관리 계획

현재 `S1-`, `S2-` 형식의 스냅샷을 Chapter와 Tutorial 기준으로 다시 정리한다.

제안 형식:

```text
C02-T01-AppModel-00.swift
C02-T02-AppModel-01.swift
C02-T02-AppModel-02.swift
C03-T03-HandTrackingService-04.swift
```

관리 규칙:

- `Cxx`는 Chapter 번호다.
- `Txx`는 Chapter 안의 Tutorial 번호다.
- 마지막 숫자는 같은 파일의 누적 변경 순서다.
- 새 파일의 첫 스냅샷은 `reset: true`다.
- 이후 파일은 직전 스냅샷을 `previousFile`로 사용한다.
- 파일 이름 변경은 이전 파일과의 관계를 설명하는 별도 Step으로 처리한다.
- 최종 스냅샷과 실제 앱 소스의 차이를 자동 또는 수동으로 비교한다.
- 실제 앱 코드가 문서보다 복잡한 경우 임의로 앱 코드를 바꾸지 않고 차이를 먼저 기록한다.

## 10. 수정 예정 파일과 산출물

### 기존 파일 수정

- `HandConstellation/HandConstellation.docc/HandConstellationTutorials.tutorial`
  - 1개 Chapter와 1개 TutorialReference를 7개 Chapter와 다수의 TutorialReference로 변경
- `HandConstellation/HandConstellation.docc/BuildingHandConstellation.tutorial`
  - 기존 URL을 가능한 한 유지하기 위해 과정 소개 또는 Chapter 1의 첫 페이지로 재사용
  - 긴 본문은 새 튜토리얼 파일로 분리
- `DocumentationTheme/index-template.html`
  - 코드 자동 스크롤 스크립트 연결
- `DocumentationTheme/css/handconstellation-theme.css`
  - Chapter·페이지 분할 후 필요한 범위에서만 레이아웃 조정
- `README.md`
  - Chapter 수, 튜토리얼 페이지 수, 예상 학습 시간 갱신

### 새 파일 추가

- Chapter별 `.tutorial` 파일 약 22~25개
- Chapter·Tutorial 기반의 누적 코드 스냅샷
- `DocumentationTheme/js/handconstellation-step-sync.js`
- Xcode 캡처 및 개념 이미지 약 18~24개

### 삭제 또는 정리

- 기존 스냅샷은 새 튜토리얼에서 참조하지 않는 것이 확인된 뒤에만 정리한다.
- 기존 공개 URL과 외부 링크가 깨지지 않도록 `BuildingHandConstellation` 식별자는 우선 보존한다.
- 파일 삭제가 필요하면 참조 검사와 DocC 빌드 성공 후 별도 변경 목록으로 확인한다.

## 11. 실행 순서

### 1단계. 현재 콘텐츠 감사

- 현재 78개 Step을 학습 목표별로 분류한다.
- 각 Step이 새 파일 생성, 기존 파일 수정, 개념 설명, 실행, 검증 중 어디에 속하는지 표시한다.
- 실제 앱 소스와 최종 튜토리얼 스냅샷의 차이를 확인한다.
- `ConstellationConfiguration.swift`처럼 현재 튜토리얼에 빠진 실제 파일을 확인한다.

산출물: 기존 Step에서 새 Chapter·Tutorial로 이동하는 대응표.

### 2단계. Chapter와 튜토리얼 골격 작성

- `HandConstellationTutorials.tutorial`에 7개 Chapter를 만든다.
- Chapter 순서대로 `@TutorialReference`를 연결한다.
- 각 튜토리얼 파일에 제목, 시간, Intro, Section, 빈 Steps 구조를 만든다.
- 이전·다음 이동 순서와 기존 URL 보존 방식을 확인한다.

산출물: 코드가 아직 없는 전체 목차와 페이지 이동 구조.

### 3단계. 설명과 코드 Step 재작성

- 한 Step 한 행동 원칙으로 기존 내용을 다시 나눈다.
- 새 파일 생성 페이지와 기존 파일 수정 페이지를 분리한다.
- 코드 스냅샷을 Chapter·Tutorial 기준으로 다시 연결한다.
- 각 페이지에 시작 목표, 결과 확인, 오류 안내를 추가한다.
- Chapter 마지막에 짧은 Assessment를 배치한다.

산출물: 텍스트와 코드가 연결된 전체 튜토리얼.

### 4단계. 이미지 추가

- Xcode 조작 화면을 실제 프로젝트에서 캡처한다.
- 기존 개념 이미지의 내용과 표현을 새 순서에 맞게 수정한다.
- 새로운 좌표 변환, 파일 책임, RealityKit 구조 그림을 추가한다.
- 실제 Vision Pro 캡처가 필요한 항목을 별도로 확인한다.

산출물: 페이지별 시각 자료와 대체 텍스트.

### 5단계. 코드 스크롤 동기화 구현

- 독립 테마 스크립트를 추가한다.
- 현재 Step 변경과 오른쪽 코드 렌더 완료를 감지한다.
- 강조 코드 블록이 보이도록 코드 패널만 이동한다.
- 데스크톱, 좁은 화면, 이미지 Step, 긴 코드에서 테스트한다.

산출물: 현재 코드가 자동으로 보이는 DocC 테마.

### 6단계. 빌드와 시각 검증

- DocC 빌드와 diagnostics를 확인한다.
- 모든 `@TutorialReference`, 코드, 이미지 참조를 검사한다.
- 첫 Chapter부터 마지막 Chapter까지 순서대로 이동한다.
- 대표 페이지를 큰 화면과 작은 화면에서 확인한다.
- 강조 줄, 파란 현재 Step, 코드 자동 스크롤을 검증한다.
- 실제 앱 소스가 의도치 않게 변경되지 않았는지 확인한다.

산출물: 빌드 가능한 최종 DocC와 검증 결과.

## 12. 검증 체크리스트

### 구조

- [ ] 목차에 7개 Chapter가 올바른 순서로 표시된다.
- [ ] 각 Chapter에 연결된 튜토리얼 페이지가 모두 열린다.
- [ ] 이전·다음 이동 순서가 목차 순서와 같다.
- [ ] 기존 `BuildingHandConstellation` 링크 처리 방침이 적용되어 있다.

### 학습 흐름

- [ ] 새 파일 생성 페이지와 기존 파일 수정 페이지가 분리되어 있다.
- [ ] 한 Step이 여러 독립 행동을 동시에 요구하지 않는다.
- [ ] 어려운 용어는 코드보다 먼저 설명한다.
- [ ] 각 페이지에 목표, 결과 확인, 문제 해결 안내가 있다.
- [ ] Vision Pro 실행은 Chapter 7에만 있다.

### 코드

- [ ] 모든 코드 참조 파일이 존재한다.
- [ ] 모든 `previousFile` 연결이 올바르다.
- [ ] 새로 추가된 줄만 파란색으로 강조된다.
- [ ] 페이지 사이의 누적 코드가 끊기지 않는다.
- [ ] 최종 스냅샷과 실제 앱 코드의 차이가 설명되거나 제거되어 있다.

### 화면

- [ ] 현재 Step의 왼쪽 파란 표시가 유지된다.
- [ ] 현재 Step의 강조 코드가 오른쪽 패널에 자동으로 보인다.
- [ ] 자동 이동 후 사용자가 코드를 직접 스크롤할 수 있다.
- [ ] 코드가 없는 Step에서 불필요한 이동이 발생하지 않는다.
- [ ] 모바일 코드 모달이 기존처럼 동작한다.
- [ ] 이미지가 잘리지 않고 대체 텍스트가 제공된다.

### 빌드와 기기

- [ ] DocC가 오류와 경고 없이 빌드된다.
- [ ] DocC diagnostics에 누락된 참조가 없다.
- [ ] 기존 앱과 테스트가 문서 변경의 영향을 받지 않는다.
- [ ] Apple Vision Pro에서 권한 허용 흐름을 확인한다.
- [ ] 다섯 점의 첫 별자리와 분리된 두 번째 별자리를 확인한다.
- [ ] 그리기 꺼짐 상태에서 새 점이 생기지 않는다.

## 13. 위험 요소와 대응

| 위험 | 대응 |
| --- | --- |
| 튜토리얼 페이지가 너무 많아짐 | 관련된 새 파일은 하나의 생성 전용 페이지에서 함께 준비하고 Chapter당 3~4개 페이지를 목표로 조정 |
| 페이지 분할 후 누적 코드가 끊김 | Chapter·Tutorial 기반 스냅샷 이름과 대응표를 먼저 만든 뒤 본문 작성 |
| 커스텀 스크립트가 DocC 업데이트에 영향을 받음 | 해시가 붙은 번들을 직접 수정하지 않고 작은 독립 스크립트와 제한된 DOM 선택자 사용 |
| 자동 스크롤이 사용자 조작을 방해함 | Step이 실제로 바뀐 순간에 한 번만 실행하고 수동 스크롤 중에는 반복 실행하지 않음 |
| 실제 Vision Pro 캡처가 늦어짐 | 캡처가 필요한 Step과 파일 이름을 먼저 고정하고 공개 전 실제 이미지로 교체 |
| 튜토리얼 코드와 앱 소스가 다름 | 실제 앱 코드를 자동으로 덮어쓰지 않고 차이를 기록한 뒤 문서 또는 앱 변경 방향을 별도로 결정 |
| 기존 공개 링크가 깨짐 | 기존 `BuildingHandConstellation` 식별자를 재사용하고 새 페이지 추가 방식 우선 적용 |

## 14. 이번 개편의 범위 밖

- 손 추적 알고리즘 자체 변경
- 새로운 앱 기능 추가
- 점과 선의 디자인 변경
- Vision Pro 제스처 인식 기준값 조정
- 앱 아키텍처 전면 변경
- GitHub Pages 배포 방식 변경

튜토리얼을 작성하면서 실제 앱의 결함이나 문서와의 불일치가 발견되면 이 개편에 임의로 포함하지 않고 별도 수정 제안으로 보고한다.

## 15. 최종 완료 조건

다음 조건을 모두 만족하면 개편을 완료한 것으로 본다.

1. 전체 과정이 7개의 Chapter로 나뉘어 목차에 표시된다.
2. 각 Chapter는 짧은 독립 튜토리얼 페이지로 진행된다.
3. 새 파일 생성과 기존 파일 수정이 서로 다른 페이지에 있다.
4. 현재 Step의 강조 코드가 오른쪽 패널에 자동으로 표시된다.
5. 모든 어려운 개념에 선행 설명과 시각 자료가 있다.
6. Vision Pro 실행과 권한 허용이 독립 Chapter에 있다.
7. 첫 별자리와 두 번째 별자리를 만드는 최종 검증 절차가 있다.
8. DocC가 누락된 참조와 diagnostics 없이 빌드된다.
9. 문서 변경으로 실제 앱 소스에 의도치 않은 변경이 생기지 않는다.
