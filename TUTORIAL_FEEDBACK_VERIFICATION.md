# 튜토리얼 피드백 수정·검증 기록

검증일: 2026-09-13. [상세 수정 계획](TUTORIAL_FEEDBACK_IMPLEMENTATION_PLAN.md)에 따라 로컬 수정과 검증을 수행했다. 공개 GitHub Pages에는 아직 배포하지 않았다.

## 피드백별 원인과 반영 결과

| 피드백 | 확인한 원인 | 반영한 수정 |
| --- | --- | --- |
| Info에 visionOS가 안 보이고 권한 키를 확인하기 어렵다 | 실제 Xcode 26.6에서도 visionOS 타깃의 표 제목이 Custom iOS Target Properties였다. 표 제목을 성공 조건으로 삼은 안내가 잘못됐다. | 타깃과 `NSHandsTrackingUsageDescription` 값을 확인하도록 변경. 실제 표시 이름 Privacy - Hands Tracking Usage Description과 Xcode 캡처 추가. Info.plist 파일이 없는 구성도 안내. |
| 만들지 않은 AppModel을 먼저 찾으라고 한다 | 선행 파일 안내와 실제 생성 순서가 맞지 않았고, 생성된 문서의 다음 링크도 목차를 건너뛰었다. | 파일 생성을 구현 페이지 첫 단계로 이동. 목차를 기준으로 다음 링크와 Chapter 소속을 정규화. |
| 프리뷰 없이 화면을 확인하라고 한다 | 결과 이미지와 Xcode Preview의 역할이 불분명했고, 첫 단계에 자료가 없으면 이후 코드까지 비는 표시 오류가 있었다. | 실제 Simulator 대기 화면을 제어창 완성 단계와 웹 Preview에 연결. 각 단계에서 확인할 수 있는 결과와 실행 환경을 구분. 첫 유효 자료 표시와 코드 패널 너비·스크롤 수정. |
| 내용이 적어 독립 페이지의 필요성이 없다 | 파일 준비만 분리된 페이지가 많았다. 별도로, 내용이 있는 페이지도 테마 오류로 소개만 보였다. | 준비 페이지 6개를 구현에 통합해 25개를 19개로 정리. AppModel·제어창·기존 점 연결에 단계별 코드 추가. 빈 섹션 소개를 처리하지 못하는 테마 수정. |

사용자의 “None”은 내용이 빈약해 페이지 분리가 불필요하다는 뜻으로 반영했다. 문자 None을 찾아 바꾸는 작업으로 처리하지 않았다.

## 추가로 바로잡은 부분

- 한국어 Chapter 경로 충돌 때문에 생긴 다음 페이지 누락과 잘못된 Chapter 이름을 생성 후 교정한다. 기존 주소 6개는 통합된 페이지로 연결한다.
- Safari의 단계 감지 범위를 화면 높이 기준으로 바꿔 스크롤에 맞춰 코드가 바뀌도록 했다. 코드 내부 스크롤과 페이지 스크롤을 분리하고 Preview가 패널 밖으로 밀리지 않도록 했다.
- 주먹으로 그리기를 전환한다고 남아 있던 그림을 버튼 기반 상태 전환 그림으로 교체했다.
- Chapter 3 코드가 Chapter 4에서 정의하는 좌표 도우미에 미리 의존하지 않도록 했다. 도우미 적용 단계는 정의 뒤로 옮겼다.
- 코디네이터 중간 단계의 미구현 메서드를 명시적인 임시 빈 메서드로 제공해 해당 시점의 컴파일 안내와 맞췄다.
- 하나의 단계에 코드 파일 여러 개가 들어 있던 부분을 분리했다. 최종 코드 11개는 실제 앱 소스와 일치한다.

## 검증 결과

| 검사 | 결과 |
| --- | --- |
| DocC 빌드 | visionOS SDK, Release, 경고를 오류로 처리한 빌드 통과 |
| 본문과 생성 문서 검사 | 실습 19개, Chapter 7개, 다음 링크 18개, 기존 주소 6개 통과 |
| 자료 참조 | 본문·이전 코드·그림·웹 Preview 참조 및 불필요 자료 검사 통과 |
| 예제 코드 | 스냅샷 95개 생성, Swift 스냅샷 문법 검사와 최종 소스 일치 검사 통과 |
| 기존 동작 테스트 | `swift test` 23개 통과, 실패 0개 |
| 중간 단계 타입 검사 | AppModel 5개, ControlView 4개, 기존 점 연결 6개 및 Chapter 3 서비스·Chapter 6 코디네이터의 선행 파일 조합 통과 |
| Simulator | 앱 빌드·설치·실행 후 대기 상태 제어창 캡처 |
| Safari | 목차, 권한→AppModel 이동, AppModel·제어창의 본문 표시, 단계별 코드 전환, 완성 단계 Preview 표시, 기존 점 연결의 8개 단계와 다음 링크 확인 |
| 그림 | 새 버튼 전환 그림을 렌더링해 문구와 배치 확인 |

Safari에서는 실제 공개 사이트의 문제를 확인한 뒤 생성된 로컬 사이트에서 주요 회귀 경로를 확인했다. 모든 페이지의 모든 화면 크기를 수동 점검했다는 의미는 아니다. 전체 문서의 구조와 참조는 자동 검사로 보완했다.

## 유지·재검증 방법

```sh
./Scripts/verify-snapshots.sh
python3 Scripts/normalize_docc_navigation.py /path/to/HandConstellation.doccarchive
python3 Scripts/verify_tutorial.py --archive /path/to/HandConstellation.doccarchive
```

스냅샷 검사 기본 동작은 읽기 전용이다. 재생성할 때만 `--generate`를 붙인다. 문서 빌드 직후에는 항상 정규화와 아카이브 검사를 실행한다. GitHub Actions에도 같은 순서를 반영했다.

테마의 `topic.264624e6.js`는 배포된 DocC 번들에 대한 제한적인 수정이다. 테마를 새 버전으로 교체할 때 빈 소개 처리, 최초 자료 선택, 단계 감지와 관련 CSS를 재확인해야 한다.

## 남은 확인 범위

### 로컬 리뷰 후 화면 조정

사용자의 코드 잘림 피드백을 반영해 페이지 좌우 여백과 본문 전체 폭은 기존 테마 값을 유지하고, 공개 참고 페이지와 동일하게 설명 폭 37.5%, 간격 4.1667%, 코드 폭 calc(50vw + 8.33333%)·최대 921px를 적용했다. 긴 코드 줄은 줄 번호를 유지하며 줄바꿈한다. 자료가 없는 섹션도 설명을 전체 폭으로 늘리지 않고 오른쪽 패널 영역을 유지한다. Safari에서 넓어진 코드와 기존에 잘리던 주석의 끝부분을 확인했고, 아카이브 검사도 다시 통과했다. 스타일 주소에 버전을 붙여 이전 CSS 캐시가 남지 않도록 했다.

실제 Apple Vision Pro의 권한 팝업, 손 추적, 기존 점 연결, 별자리 분리는 이번 Simulator 캡처로 검증하지 않았다. 해당 실기기 확인은 Chapter 7의 절차에 따라 진행해야 한다. 실제 캡처 2개 외의 설명 그림은 개념도이며 실기기 실행 증거가 아니다.

변경사항은 로컬 작업 트리에 있다. 원격 push와 공개 사이트 배포는 수행하지 않았다.
