# 튜토리얼 피드백 수정 계획

작성: 2026-09-12. 범위: 튜토리얼 내용, 문서 테마, 문서 생성·검증 절차. 앱의 손 추적·그리기 동작은 이번 수정의 대상이 아니다.

## 1. 실제 Safari에서 확인한 문제

- 목차와 하단 다음 링크가 다르다. 권한 설명 다음에 AppModel을 건너뛰고 HandTrackingService로 이동한다.
- 공유 상태 페이지의 두 번째 섹션, 제어창·입력 파일 준비·기존 점 재연결 페이지의 구현 섹션이 표시되지 않는다. 원문과 생성 데이터에는 내용이 있다.
- 첫 단계에 코드나 이미지가 없는 섹션에서 뒤 단계의 미디어가 표시되지 않는 사례가 있다.
- 소개의 제목과 배경 도해의 글자가 겹친다. 설명 카드 옆에 불필요하게 넓은 빈 공간이 남는다.
- 반복되는 파일 생성이 독립 페이지를 차지하고, 주요 구현은 완성 코드를 한 번에 제공한다.
- 권한 설정은 특정 표 제목과 실제 Info.plist 파일의 존재를 가정한다.
- 아직 연결하지 않은 기능의 동작과 준비하지 않은 테스트 환경을 완료 조건으로 요구한다.

## 2. 작업 순서와 담당

세 서브 에이전트 모두 GPT-5.6 Sol / medium으로 실행한다. 파일 소유 범위를 나눠 동시에 작업하고 주 에이전트가 통합한다.

| 순서 | 담당 | 작업 | 산출물 |
| --- | --- | --- | --- |
| 1 | 주 에이전트 | 실제 사이트 기준 문제·수정 범위·완료 조건 확정 | 이 계획, Safari 확인 기록 |
| 2A | 표시 담당 | 빈 섹션 도입부 처리, 단계별 미디어 표시, 도해·본문 배치 개선 | 문서 테마 수정 |
| 2B | 내용 담당 | 페이지 통합, 단계별 코드, 권한·선행조건·결과 확인 수정 | 튜토리얼과 재현 가능한 코드 스냅샷 |
| 2C | 검증 담당 | 생성된 문서의 순서 정규화, 자료와 본문 검증, 배포 전 검사 | 검사 도구와 문서 배포 절차 |
| 2D | 주 에이전트 | 그림·프리뷰 자료 확인, 사용 문서 갱신 | 정확한 화면 자료와 검증 설명 |
| 3 | 주 에이전트 | 각 담당 결과를 통합하고 실제 문서 생성 | 로컬 미리보기 사이트 |
| 4 | 주 에이전트 | Safari에서 하단 링크 이동, 구현·코드·그림·반응형 표시 확인 | 검증 결과와 남은 제한 기록 |

## 3. 페이지 통합안

7개 Chapter는 유지하고 25개 페이지를 19개 실습 페이지로 정리한다. 제거된 URL은 가능한 한 해당 실습의 시작 페이지로 연결한다.

| 기존 페이지 | 통합 위치 |
| --- | --- |
| 새 파일 준비하기: 앱 상태 | 공유 상태 모델 구현의 첫 섹션 |
| 기존 ContentView를 ControlView로 바꾸기 | 제어창 구현의 첫 섹션 |
| 새 파일 준비하기: HandTrackingService | 손 추적 세션 시작의 첫 섹션 |
| 새 파일 준비하기: 설정값과 입력 감지기 | 설정값, 체류, 기존 점 연결의 각 첫 단계 |
| 새 파일 준비하기: 별자리 데이터와 화면 | 모델, 렌더러의 각 첫 단계 |
| 새 파일 준비하기: 몰입형 흐름 | 코디네이터, RealityView의 각 첫 단계 |

기존 점 재연결은 독립 학습 주제로 유지한다. 후보 제외 → 출발점 이탈 → 최근접 선택 → 목표 고정 → 체류 확정 → 취소·초기화 순서로 구현한다.

## 4. 페이지별 작성 기준

- 시작: 필요한 파일·앞 단계, 이번 페이지의 산출물, 확인 가능한 환경을 설명한다.
- 본문: 개념 설명은 일반 본문으로, 실제 변경은 작은 코드 단계로 구분한다.
- 코드: 수정할 파일과 위치를 지정하고 이전 코드 대비 바뀐 부분이 드러나게 한다.
- 완료: 해당 시점에 실제로 가능한 빌드·화면·입력 결과만 확인한다.
- 복구: 흔한 오류에 대해 확인할 설정이나 코드를 직접 알려 준다.
- 최종 코드: 실제 앱 소스와 일치시키고 생성기를 다시 실행해도 같은 결과가 나와야 한다.

AppModel은 상태 타입·초기값·전환·안내·초기화로 나눈다. 제어창은 파일 이름·환경 연결 → 화면 정보 → 안내 → 버튼 → 상태 표현 → 몰입형 요청으로 나눈다. Chapter 2에서는 UI 표시까지 확인하고, 실제 손 추적과 공간 동작은 연결 이후 확인한다.

## 5. 권한 설정과 시각 자료

- 표 제목이 아닌 visionOS 타깃과 NSHandsTrackingUsageDescription 설정값을 확인한다.
- Info.plist가 보이는 경우에만 XML 확인을 안내한다. 파일이 안 보인다고 실패로 판단하지 않는다.
- 실제 Xcode·Simulator 캡처를 확보할 수 있으면 사용한다. 확보하지 못한 그림은 개념도라고 명시한다.
- 실제 권한 창·손 추적 장면은 Vision Pro 확인이 필요하다. 합성 이미지를 실제 화면으로 표시하지 않는다.
- 웹 문서의 결과 그림과 Xcode #Preview의 역할을 구분한다.
- 제목 배경은 읽어야 할 도해를 가리지 않아야 하며, 도해는 본문에서 읽을 수 있어야 한다.

## 6. 완료 기준

1. 모든 실습 페이지가 실제 작업 섹션을 표시하고, 소개만 남은 페이지가 없다.
2. 목차 순서와 하단 다음 링크·Chapter 표시가 일치한다.
3. 본문, 코드, 이전 코드, 그림과 별칭 URL의 참조가 해결된다.
4. AppModel·제어창·재연결이 작은 작업 단계로 구성된다.
5. 스냅샷 재현성·최종 앱 소스 일치·Swift 구문 검증이 통과한다.
6. 생성 문서에 대한 검사와 배포 전 검사가 통과한다.
7. Safari에서 주요 페이지를 스크롤하고, 코드가 없는 첫 단계 이후 코드·그림이 정상 표시되는지 확인한다.
8. 완료된 확인과 실기기 없이는 확인하지 못한 항목을 결과 문서에서 구분한다.

## 7. 배포 경계

이번 작업은 로컬 수정과 검증까지 수행한다. 원격 push나 공개 사이트 배포는 하지 않으며, 검증된 변경사항과 미리보기를 결과로 제공한다.
